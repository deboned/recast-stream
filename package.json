{
  "name": "recast-stream",
  "version": "0.0.1",
  "description": "Pipe recast through streams.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Paul Apostol",
  "license": "ISC",
  "dependencies": {
    "recast": "^0.10.11",
    "through2": "^0.6.3",
    "vinyl-fs": "^1.0.0"
  }
}
